<% title "News page" %>

<% form_for [@show_news_posts_template], :builder => AdminFormBuilder, :url => { :controller => :show_news_posts_template, :action => :update } do |f| %>
  <fieldset class="layout">
    <legend>News Template</legend>
    <%= f.error_messages %>
    <table>
      <tr><td><%= f.text_area :template, :rows=>30 %></td><td><%= f.text_area :css, :rows=>30 %></td></tr>
    </table>
    <div class="submit"><%= f.submit "Save" %></div>
  </fieldset>
<% end %>

