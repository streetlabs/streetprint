<% title "Show author page" %>

<% form_for [@show_author_template], :builder => AdminFormBuilder, :url => { :controller => :show_author_template, :action => :update } do |f| %>
  <fieldset>
    <legend>Author Template</legend>
    <%= f.error_messages %>
    <%= f.text_area :template, :rows=>20 %>
    <%= f.text_area :css, :rows=>20 %>
    <div class="submit"><%= f.submit "Save" %></div>
  </fieldset>
<% end %>

