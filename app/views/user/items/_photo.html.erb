<div class="new_photo">
    <% fields_for "item[photo_attributes][]", photo do |p| %>
        <p>
            <%= p.label :photo %>
            <%= p.file_field :photo%>
            <%= link_to_function "[remove]", "$(this).parent().remove()" %>
        </p>
    <% end %>
</div>