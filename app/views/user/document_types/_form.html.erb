<% form_for [@site, @document_type], :builder => AdminFormBuilder, :url => {:controller => 'user/document_types', :action => action, :subdomain => @site.title} do |f| %>
<fieldset>
  <%= f.error_messages %>
  <% if f.object.new_record? %>
  <legend>New Document Type</legend>
  <% else %>
  <legend>Edit Document Type</legend>
  <% end %>
  <%= f.text_field :name %>
  <%= f.text_area :description, :rows=>4 %>
  <div class="submit"><%= f.submit "Submit" %></div>
</fieldset>
<% end %>
