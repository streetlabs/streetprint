<p>
  <% authors = Author.find(:all, :conditions => {:site_id  => @site.id}).map { |author| [author.name, author.id] } %>
  <% options = options_for_select(authors) %>
  <%= select_tag "item[authors_list][]", options %>
  <%= link_to_function "[remove]", "remove_parent_element($(this))" %>
</p>