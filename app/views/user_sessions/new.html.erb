<% title "Login", false %>

<% form_for @user_session, :url => user_session_path do |f| %>
  <fieldset>
  <legend>Login</legend>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Login" %>
  </fieldset>
<% end %>

