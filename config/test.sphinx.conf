indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3313
  log = /Users/crayment/rails/streetprint/log/searchd.log
  query_log = /Users/crayment/rails/streetprint/log/searchd.query.log
  pid_file = /Users/crayment/rails/streetprint/log/searchd.test.pid
}

source item_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = mysql
  sql_db = streetprint_test
  sql_sock = /tmp/mysql.sock
  sql_query_pre = UPDATE `items` SET `delta` = 0 WHERE `delta` = 1
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `items`.`id` * 1 + 0 AS `id` , CAST(`items`.`title` AS CHAR) AS `title`, CAST(`items`.`date` AS CHAR) AS `date`, CAST(`items`.`publisher` AS CHAR) AS `publisher`, CAST(`items`.`reference_number` AS CHAR) AS `reference_number`, CAST(`items`.`date_details` AS CHAR) AS `date_details`, CAST(`items`.`dimensions` AS CHAR) AS `dimensions`, CAST(`items`.`pagination` AS CHAR) AS `pagination`, CAST(`items`.`illustrations` AS CHAR) AS `illustrations`, CAST(`items`.`location` AS CHAR) AS `location`, CAST(`items`.`notes` AS CHAR) AS `notes`, CAST(`items`.`city` AS CHAR) AS `city`, CAST(`categories`.`name` AS CHAR) AS `category`, CAST(`document_types`.`name` AS CHAR) AS `document_type`, CAST(GROUP_CONCAT(DISTINCT `authors`.`name` SEPARATOR ' ') AS CHAR) AS `authors`, `items`.`id` AS `sphinx_internal_id`, 3207170592 AS `class_crc`, '3207170592' AS `subclass_crcs`, 0 AS `sphinx_deleted`, IFNULL(`items`.`title`, '') AS `title_sort`, IFNULL(`items`.`date`, '') AS `date_sort`, IFNULL(`items`.`publisher`, '') AS `publisher_sort`, IFNULL(`items`.`reference_number`, '') AS `reference_number_sort`, `items`.`site_id` AS `site_id`, UNIX_TIMESTAMP(`items`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`items`.`updated_at`) AS `updated_at`, `items`.`category_id` AS `category_id` FROM `items`    LEFT OUTER JOIN `categories` ON `categories`.id = `items`.category_id   LEFT OUTER JOIN `document_types` ON `document_types`.id = `items`.document_type_id   LEFT OUTER JOIN `authoreds` ON (`items`.`id` = `authoreds`.`item_id`)  LEFT OUTER JOIN `authors` ON (`authors`.`id` = `authoreds`.`author_id`)  WHERE `items`.`id` >= $start AND `items`.`id` <= $end AND `items`.`delta` = 0 GROUP BY `items`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `items` WHERE `items`.`delta` = 0
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = site_id
  sql_attr_uint = category_id
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = date_sort
  sql_attr_str2ordinal = publisher_sort
  sql_attr_str2ordinal = reference_number_sort
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `items` WHERE `id` = (($id - 0) / 1)
}

index item_core
{
  source = item_core_0
  path = /Users/crayment/rails/streetprint/db/sphinx/test/item_core
  charset_type = utf-8
}

source item_delta_0 : item_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = mysql
  sql_db = streetprint_test
  sql_sock = /tmp/mysql.sock
  sql_query_pre = 
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `items`.`id` * 1 + 0 AS `id` , CAST(`items`.`title` AS CHAR) AS `title`, CAST(`items`.`date` AS CHAR) AS `date`, CAST(`items`.`publisher` AS CHAR) AS `publisher`, CAST(`items`.`reference_number` AS CHAR) AS `reference_number`, CAST(`items`.`date_details` AS CHAR) AS `date_details`, CAST(`items`.`dimensions` AS CHAR) AS `dimensions`, CAST(`items`.`pagination` AS CHAR) AS `pagination`, CAST(`items`.`illustrations` AS CHAR) AS `illustrations`, CAST(`items`.`location` AS CHAR) AS `location`, CAST(`items`.`notes` AS CHAR) AS `notes`, CAST(`items`.`city` AS CHAR) AS `city`, CAST(`categories`.`name` AS CHAR) AS `category`, CAST(`document_types`.`name` AS CHAR) AS `document_type`, CAST(GROUP_CONCAT(DISTINCT `authors`.`name` SEPARATOR ' ') AS CHAR) AS `authors`, `items`.`id` AS `sphinx_internal_id`, 3207170592 AS `class_crc`, '3207170592' AS `subclass_crcs`, 0 AS `sphinx_deleted`, IFNULL(`items`.`title`, '') AS `title_sort`, IFNULL(`items`.`date`, '') AS `date_sort`, IFNULL(`items`.`publisher`, '') AS `publisher_sort`, IFNULL(`items`.`reference_number`, '') AS `reference_number_sort`, `items`.`site_id` AS `site_id`, UNIX_TIMESTAMP(`items`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`items`.`updated_at`) AS `updated_at`, `items`.`category_id` AS `category_id` FROM `items`    LEFT OUTER JOIN `categories` ON `categories`.id = `items`.category_id   LEFT OUTER JOIN `document_types` ON `document_types`.id = `items`.document_type_id   LEFT OUTER JOIN `authoreds` ON (`items`.`id` = `authoreds`.`item_id`)  LEFT OUTER JOIN `authors` ON (`authors`.`id` = `authoreds`.`author_id`)  WHERE `items`.`id` >= $start AND `items`.`id` <= $end AND `items`.`delta` = 1 GROUP BY `items`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `items` WHERE `items`.`delta` = 1
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = site_id
  sql_attr_uint = category_id
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_str2ordinal = title_sort
  sql_attr_str2ordinal = date_sort
  sql_attr_str2ordinal = publisher_sort
  sql_attr_str2ordinal = reference_number_sort
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `items` WHERE `id` = (($id - 0) / 1)
}

index item_delta : item_core
{
  source = item_delta_0
  path = /Users/crayment/rails/streetprint/db/sphinx/test/item_delta
}

index item
{
  type = distributed
  local = item_delta
  local = item_core
}
